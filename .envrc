# Load .env file if it exists (for environment variables)
# This will automatically load environment variables when entering this directory
if [ -f .env ]; then
  set -a
  source .env
  set +a
fi

# Activate Python virtual environment
source env/bin/activate
